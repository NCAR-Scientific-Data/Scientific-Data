//Qunit Hello Test
QUnit.test( "hello test", function( assert ) {
assert.ok( 1 == "1", "Passed!" );
});

//Calculating Module
QUnit.test("calculate", function( assert ) {
	expect(0);
});

//Plotting Module
QUnit.test("plot", function( assert ) {
	expect(0);
});